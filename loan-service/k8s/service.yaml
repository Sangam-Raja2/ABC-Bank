apiVersion: v1
kind: Service
metadata:
  name:  loan-service
  namespace: abcbank
  labels:
    app:  loan-service
spec:
  type: ClusterIP
  selector:
    app:  loan-service
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
