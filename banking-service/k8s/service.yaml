apiVersion: v1
kind: Service
metadata:
  name: banking-service
  namespace: abcbank
  labels:
    app: banking-service
spec:
  type: ClusterIP
  selector:
    app: banking-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
